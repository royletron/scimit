{
  "name": "scimmy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w scim-watch-backend\" \"npm run dev -w scim-watch-frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
